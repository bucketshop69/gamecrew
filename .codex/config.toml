# Project-scoped defaults for the main Codex task and inherited sub-agents.

[agents]
# Root orchestrator plus at most three concurrent sub-agents.
max_threads = 3
# Keep orchestration centralized in the root task.
max_depth = 1
interrupt_message = true
