# Base URL for the GameCrew API (see apps/api). Required at build time for
# non-development builds -- the app throws at first API call if this is
# unset outside __DEV__ (see src/api/gamecrew.ts).
EXPO_PUBLIC_GAMECREW_API_URL=http://localhost:8787
